# Articles (Markdown) — Phase 4

Dossier préparé pour les articles du blog en Markdown, gérés via **Decap CMS** (Phase 4).

Pour l'instant, le contenu des articles est statique dans `src/data/articles.js`
(champ `corps`). En Phase 4, chaque article aura son fichier `[slug].md` ici, lu
automatiquement par le CMS.
